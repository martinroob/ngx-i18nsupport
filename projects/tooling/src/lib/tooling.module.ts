import { NgModule } from '@angular/core';
import { ToolingComponent } from './tooling.component';

@NgModule({
  imports: [
  ],
  declarations: [ToolingComponent],
  exports: [ToolingComponent]
})
export class ToolingModule { }
